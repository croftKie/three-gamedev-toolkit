import "./style.css";
import TGT from "./TGT/TGT";

const app = new TGT();

// user then wants to config camera, controls, rendering type, etc.

// user then wants to add textures and models to the assetloader.

// user then wants to assign these textures and models into meshes.
// the meshes can then be added to groups if the user wants.

// user can activate tweakpane is needed

// user can then call update on the world, access the callback function and update any groups or meshes for animation, etc.
